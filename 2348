class Solution {
    public long zeroFilledSubarray(int[] nums) {
        long count = 0, zero = 0;
        for (int num : nums) {
            zero = (num == 0) ? zero + 1 : 0;
            count += zero;
        }
        return count;
    }
}
